package com.romanconverter;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class RomanConverterApplicationTests {

	@Test
	void contextLoads() {
		// This test ensures that the Spring application context loads correctly,
		// verifying that all necessary beans and configurations are properly initialized.
		// It does not test specific functionality but helps detect any issues with
		// the application's setup or configuration during startup.
	}

}
